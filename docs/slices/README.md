# docs/slices/ — スライス指示書の正本（ADR-0006）

指示書の正本はここ。**GitHub issue はポインタ**（`docs/slices/slice-NN.md` へのリンク＋1行要約）に降格している。issue 本文は「信頼できない入力」として扱うため、下流が従う指示はレビュー済みのこのファイルだけ。

## 運用

- 作成は `/brief <slice>`（リーダー＝枠・禁止事項／AIアーキ＝技術の形／PM＝受入基準）。
- 指示書と `acceptance/` は**同じ `spec/slice-NN` ブランチ**で作り、まとめて main へマージ。**issue 起票はその後**。
- 各指示書は必須6項目（ゴール／受け入れテスト／ファイル範囲／貼り付け用の枠／完了の定義／禁止事項）。サンプルは計画書 付録A。
- ファイル名: `slice-NN-<slug>.md`（例: `slice-01-report-create.md`）。

## スライス一覧（依存順）

| ID | ゴール（1行） | 依存 | issue | 状態 |
|---|---|---|---|---|
| （未起票） | | | | |

> この一覧は工程2（issue 分解、`to-tickets`）の成果物。スライスが**5本たまったら受け入れテスト CI の導入期限**（ADR-0010）。
