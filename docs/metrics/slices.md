# スライス記録（プロセスKPI 生ログ）— 非推奨（P1で events へ移行決定・2026-07-11）

> **status: deprecated。** `docs/metrics/events/**`（NDJSON・append-only）が正本に置き換わった
> （実装ロードマップ P1 §3 項目5）。本ファイルには記録が一切無いため変換スクリプトは不要と判断し、
> 単純に非推奨化した。`.claude/skills/submit/SKILL.md` の追記先切替は P3（`/rescue` Actions実装）と
> 合わせて別途対応する（未着手）。

`/submit` が1スライス完了ごとに1行追記する。集計方法とカラムの定義は同ディレクトリの `README.md` を参照。
**`救援有無` はリーダーが窓口対応時に埋める**（初級の自己申告に頼らない）。空欄は未記入を意味する。

| slice_id | 日付 | 救援有無(y/n) | 再作成回数 | 枠消費 | 差し戻し理由 | メンバー |
|---|---|---|---|---|---|---|
