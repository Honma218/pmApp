# 層境ゲート判定ログ（ADR-0007 / 改訂 2026-07-11）

> **status: 本運用（2026-07-11・P0 #6 GO）。** ADR-0007 改訂（NO-GO 種別の必須化）反映済み。
>
> **P6準備（2026-07-11）で `/gate`・`/reject` コメントコマンドを実装した**（`.github/workflows/gate.yml`）。
> KPI 計算（`fold`）が読むのは `docs/metrics/events/**` の `gate`/`rejected` イベントであり、
> この表そのものではない。**この表は人間が読みやすい形の任意の手動ログ**として残す
> （`/gate` を打ち忘れた場合の事後記録・目視での突合用）。両者が食い違う場合は
> `docs/metrics/events/**` が正（正本は events）。

全 PR に層境ゲートが掛かる（ADR-0007）。**判定者は PM**（代理はリーダー1名。GATE_KEEPERS で強制）。
PM は `/gate GO` などのコメント（自動記録）に加え、任意でこの表にも1行残せる。
**NO-GO のときは種別（`rework`/`redecompose`）を必ず埋める**——`/gate` コマンドでは必須入力、
この表では CI 検証の対象外（あくまで手動ログのため）。

## 記録ルール

- `判定` は `GO` / `NO-GO`。
- `NO-GO種別`：判定が `NO-GO` のとき **必須**。`GO` のときは空欄。
  - `rework` … 修正で足りる。**同一 slice_id を継続**。→ 差し戻し率の分子。
  - `redecompose` … 分解し直し。**子スライスを新規 slice_id で起票**。→ 肥大率側（分解の失敗）。
- `代理`：リーダーが代理判定した場合に `リーダー` と記す（PM が事後確認・ADR-0007）。通常は空欄。
- 種別を分けるのは、両者の原因が異なる（実装の雑さ vs 分解の雑さ）ため。混ぜると CLAUDE.md §3 の2分類原則が働かない。

## 判定ログ

| slice_id | 日付 | 判定 | NO-GO種別 | 代理 | PM | 備考 |
|---|---|---|---|---|---|---|
| | | | | | | |

<!--
記入例（削除しないこと）:
| 7 | 2026-07-20 | NO-GO | rework      |        | pm | 422 を 400 で返していた |
| 7 | 2026-07-21 | GO    |             |        | pm | 再検証緑・差分閾値内 |
| 9 | 2026-07-22 | NO-GO | redecompose | リーダー | -  | 範囲が広すぎ。子9a/9bへ分割 |
-->
