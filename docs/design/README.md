# docs/design/ — 設計ドキュメント

Step 0 で AIアーキが AI に `reference-mock/` を読ませて **`overview.md`（システム概要設計）** をここに起こす（playbook 工程1）。以降、基本設計の成果物（API の形・データ形・画面遷移など、スライス指示書より上位の設計）を置く。

- 個別の設計決定は `docs/adr/` が正本。ここは「現在の全体像」を描く場所。
- CLAUDE.md からは progressive disclosure でここを参照させる（憲法には書かない）。
