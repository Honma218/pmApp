# Next.js サーバーから FastAPI バックエンドへ接続する先（SSR ガード / rewrites プロキシ）
BACKEND_ORIGIN=http://localhost:8000

# 補足: このプロキシ構成では、バックエンドの OAUTH_REDIRECT_URI を
# フロントのオリジンに合わせる（例: http://localhost:3000/auth/callback）と、
# セッション Cookie がフロントと同一オリジンのファーストパーティとして扱われる。
